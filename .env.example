DATABASE_URL="postgresql://user:password@localhost:5432/geladao_delivery"

PORT=3333
CORS_ORIGIN="http://localhost:3000"

# Only needed with the local `prisma dev` server (see prisma.config.ts)
SHADOW_DATABASE_URL="postgres://postgres:postgres@localhost:51215/template1?sslmode=disable"
